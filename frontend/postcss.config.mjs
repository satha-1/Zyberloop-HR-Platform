/**
 * PostCSS Configuration for Next.js with Tailwind CSS v4
 *
 * Next.js requires PostCSS config to export a `plugins` key.
 * For Tailwind CSS v4, we use the @tailwindcss/postcss plugin.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
